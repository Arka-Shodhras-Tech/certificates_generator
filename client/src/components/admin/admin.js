import { StudentRequest } from "./request"
import Submission from "./submission"

export const Admin=()=>{
    return(
        <div>
            <StudentRequest/>
            <Submission/>
        </div>
    )
}